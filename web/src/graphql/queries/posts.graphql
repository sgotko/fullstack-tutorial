query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    hasMore
    posts {
      id
      title
      textSnippet
      points
      creator {
        id
        username
        email
        createdAt
      }
      createdAt
      updatedAt
    }
  }
}
